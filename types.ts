export type User = {
    username: string
    email: string
    mobile: string
    age: string
    password: string
    }    

export type TFilm = {
    name: string
    img: string
    description: string
    tag: string []
}